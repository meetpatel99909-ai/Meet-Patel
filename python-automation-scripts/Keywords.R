fun = function(one)
{
  one.plus.one <- oen + 1
  four <- newVar <- matrix(1:10,nrow = 2) 
  four[ 1, ]
  txt <- 'hi'
  three <- two+ 1
  if(txt == 'hi') 4
  5}  
  
library("readxl")

my_data <- read_excel("my_file.xls")
# xlsx files
my_data <- read_excel("\\viral\EUC Training\CIMCON Sheet1.xlsx)
#{     #nolint

Y <- scan()
12 14 33 50 67 74 123 141 165 204 253 246 240
.system(
.run(
.Popen(
.call(
.check_call(
.startfile(
pyodbc.connect(
'PWD=
'Password=
.urlopen(
.sendmail(
open(*)
.to_excel(
.to_csv(
.read_excel(
.read_csv(
PdfFileReader(
PdfFileWriter(
library(nnet) # for class.ind
